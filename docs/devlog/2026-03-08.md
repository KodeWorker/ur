# 2026-03-08

## Done

- Pre-commit hooks all passing: trim-whitespace, end-of-file-fixer, mixed-line-ending,
  check-yaml, check-added-large-files, check-merge-conflict, cmake-format, cmake-lint,
  clang-format, cppcheck, cpplint
- Disabled uncrustify (conflicts with clang-format on Google-style class indentation)
- Added `exclude: ^third_party/` to clang-format, cppcheck, cpplint hooks to skip SQLite amalgamation
- Added cppcheck suppressions: `syntaxError` (GoogleTest macros), `constParameterReference`
- Fixed CMake project language: `project(ur C CXX)` — required for `sqlite3.c` compilation
- CMake build confirmed clean: all targets build (`sqlite3`, `ur_lib`, `ur`, test binaries)
- Reviewed and fixed `crypto.cpp`:
  - `static_cast<int>` on `plaintext.size()` and `body_len` for EVP update calls
  - `static_cast<int>` on `iv.size()` and `tag.size()` for `EVP_CIPHER_CTX_ctrl` calls
  - Buffer sized to `plaintext.size() + EVP_MAX_BLOCK_LENGTH`
  - `#include <vector>` added

## Notes (by Kodeworker)
  - To fix clang-format fails, just run `clang-format --style=file -i <path/to/file>`

## TODOs (core logic — owner: Kodeworker)

### `src/ur/memory/workspace.cpp`
- [ ] `resolve_paths()` — detect platform, resolve root, build all sub-paths
- [ ] `init_workspace()` — `create_directories` for all five subdirs
- [ ] `remove_workspace()` — `remove_all` workspace and database subdirs

### `src/ur/memory/database.cpp`
- [ ] `~Database()` — `sqlite3_close(handle_)` if not nullptr
- [ ] `open()` — `sqlite3_open()`, throw on failure
- [ ] `init_schema()` — `sqlite3_exec()` with CREATE TABLE IF NOT EXISTS for all three tables
- [ ] `drop_all()` — `sqlite3_exec()` DROP TABLE (message → session → persona)

### `src/ur/cli/command.cpp`
- [ ] `cmd_init()` — call `init_workspace` + `db.init_schema()`, print confirmation
- [ ] `cmd_clean()` — parse flag, call `remove_workspace` and/or `db.drop_all()`

### `tests/unit/test_workspace.cpp`
- [ ] Add fixture (temp dir in `SetUp`, remove in `TearDown`)
- [ ] `ResolvePathsReturnsNonEmptyRoot`
- [ ] `InitWorkspaceCreatesAllSubdirs`
- [ ] `InitWorkspaceIsIdempotent`
- [ ] `RemoveWorkspaceClearsContents`

### `tests/unit/test_crypto.cpp`
- [ ] Add temp file fixture for key file tests
- [ ] `LoadKeyReturnsEmptyWhenFileAbsent`
- [ ] `LoadKeyReturnsContentsWhenFilePresent`
- [ ] `EncryptDecryptRoundTrip`
- [ ] `DecryptFailsOnTamperedCiphertext`
- [ ] `EncryptProducesUniqueIVEachCall`

### `tests/unit/test_database.cpp`
- [ ] Add fixture (temp db path in `SetUp`, remove file in `TearDown`)
- [ ] `IsNotOpenBeforeFirstCall`
- [ ] `InitSchemaOpensDatabase`
- [ ] `InitSchemaIsIdempotent`
- [ ] `InitSchemaCreatesAllThreeTables`
- [ ] `DropAllRemovesAllTables`
