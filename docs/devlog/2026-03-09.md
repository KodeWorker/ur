# 2026-03-09

## Done

- Added `Logger` class (`src/ur/log/logger.hpp` / `logger.cpp`)
  - Levels: DEBUG, INFO, WARN, ERROR
  - Min level read from `UR_LOG_LEVEL` env var at startup (defaults to INFO)
  - Writes full timestamped lines to `$root/log/ur.log` (lazy open, append mode)
  - WARN and ERROR also echo a short `[LEVEL] message` line to stderr
  - File open failure is silent — stderr output still works
- Wired `Logger` into `Context` struct; constructed in `make_context()` via `log_level_from_env()`
- Registered `log/logger.cpp` in `src/ur/CMakeLists.txt`

- Implemented all 14 unit tests — 14/14 passing via `ctest`
  - `test_workspace`: fixture with `getpid()`-isolated temp dir; covers resolve, create, idempotent, remove
  - `test_database`: fixture with temp `.db` file; verifies schema via independent raw `sqlite3*` handle
  - `test_crypto`: split fixture (`CryptoFileTest` for key-file tests, `CryptoTest` for pure crypto); tamper test flips byte 15 (past IV) to trigger GCM auth failure

## Code review fixes (milestone #16, issues #160–#167)

- **#160** `Database`: migrated `handle_` to `std::unique_ptr<sqlite3, decltype(&sqlite3_close)>`; fixed `open()` to use raw temp pointer + `handle_.reset()`; destructor simplified
- **#161** `encrypt()`/`decrypt()`: added `key.size() != 32` guard; `load_key()`: TOCTOU fixed — open first, check existence only on failure
- **#162** `RAND_bytes`: added missing `static_cast<int>(iv.size())`
- **#163** `EVP_CIPHER_CTX`: replaced 14 manual `EVP_CIPHER_CTX_free` calls with `std::unique_ptr` RAII in both `encrypt()` and `decrypt()`
- **#164** Closed — not a bug; clarified `workspace.hpp` doc comment instead
- **#165** `Logger`: added explicit Rule of Five (`= delete` copy, `= default` move)
- **#166** `cmd_clean`: error messages now include `Usage: ur clean [--workspace|--database]`
- **#167** `load_key()`: TOCTOU race resolved (see #161)
- `remove_workspace()`: fixed to remove contents of `workspace/` but keep the directory itself; updated doc comment and test assertion
- `Paths.keys` renamed to `Paths.key`; `Paths.log` renamed to `Paths.logs`
- PR #168 opened against `main`

## Phase 1 status: COMPLETE ✓

All acceptance criteria met:
- [x] `ur init` creates all five workspace subdirs and the SQLite DB
- [x] `ur init` is idempotent
- [x] `ur clean` removes database and workspace contents
- [x] `make_context()` loads `$root/key/secret.key` if present; `enc_key` empty otherwise — key is used by `Database` enc/dec on message read/write, not by `ur init` itself
- [x] All 14 unit tests pass via `ctest --test-dir build`
