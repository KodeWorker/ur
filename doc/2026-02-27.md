# Fix #10: Structured content serialization for Message type

| File | What changed |
|---|---|
| `src/ur/agent/models.py` | Replaced `Message = dict[str, Any]` with `UserMessage`, `AssistantMessage`, `ToolMessage`, `SystemMessage` TypedDicts and a `Message` union |
| `src/ur/memory/db.py` | Added `tool_calls TEXT`, `tool_call_id TEXT`, `name TEXT` columns; `content` default changed to `'null'` |
| `src/ur/memory/session_store.py` | Added `_serialize_message` (unconditional `json.dumps`) and `_deserialize_message` (robust `json.loads` + drops None optional keys); updated INSERT and SELECT to use new columns |
| `src/ur/agent/session.py` | Removed `created_at` from in-memory message dicts — it's a DB concern, not a wire-format concern |
| `tests/unit/test_memory.py` | Added two new round-trip tests (`tool_calls` and `tool_call_id`) |
| `tests/unit/test_session.py` | Dropped the now-stale `assert "created_at" in ...` assertions |

# Fix #11: LLMClient lifecycle refactor

| File | What changed |
|---|---|
| `src/ur/llm/client.py` | Added `model: str | None = None` param to `__init__`; `self.model` now used in `stream()` instead of `self.settings.model` |
| `src/ur/agent/loop.py` | Replaced `settings: Settings` with `client: LLMClient` and `max_iterations: int`; removed internal `LLMClient` construction; removed `Settings` import |
| `src/ur/main.py` | Constructs `LLMClient(settings, model=model)` once per session; passes `client` and `settings.max_iterations` to `agent_run`; replaced static `_detect_provider` calls with `client.provider` |
| `tests/unit/test_loop.py` | Replaced all `patch("ur.agent.loop.LLMClient")` blocks with `MagicMock(spec=LLMClient)` injected directly; removed stale `created_at` assertions; removed `tmp_settings` fixture usage |

# Fix bugs #20, #21, #22, #25, #26, #30, #31

| # | File(s) | What changed |
|---|---|---|
| #20 | `src/ur/memory/session_store.py`, `src/ur/llm/client.py` | Strip `created_at` in `_deserialize_message` by default; add `with_metadata=True` opt-in; filter `_INTERNAL_KEYS` at API boundary in `LLMClient.stream()` |
| #21 | `src/ur/agent/models.py`, `src/ur/agent/session.py` | Add `created_at: NotRequired[str]` to TypedDicts; populate real creation time in `AgentSession.new()`, `add_user_message()`, `add_assistant_message()` |
| #22 | `src/ur/main.py` | Guard `_history` display against `None`/list content; fall back to `str(content)` for non-string types |
| #25 | `src/ur/llm/client.py` | Fix `_detect_provider` to split on `/` and match exact prefix; `ollama_chat/...` now correctly maps to `Provider.OLLAMA` |
| #26 | `src/ur/main.py` | Remove `session.fail()` from recoverable error path in `_chat`; reserve `fail()` for unrecoverable termination only |
| #30 | `src/ur/main.py` | Replace Python-side `startswith` filter with `SQL LIKE ? \|\| '%'`; detect ambiguous prefixes via `LIMIT 2` |
| #31 | `src/ur/memory/session_store.py` | Re-raise corrupt `tool_calls` JSON as `ValueError` with context instead of silently assigning raw string |

# Milestone 2 — Code Review Round 2

| # | Title | Severity |
|---|---|---|
| #20 | `get_session_messages` leaks `created_at` into LLM API payloads | Critical |
| #21 | `save_session` always stamps messages with save-time, not creation-time | Critical |
| #22 | `history` command crashes on non-string content | Major |
| #23 | Fix `AsyncIterator` vs `AsyncGenerator` type annotations | Major |
| #24 | `get_db` context manager should auto-commit on clean exit | Major |
| #25 | Fix provider detection to use exact prefix matching | Major |
| #26 | `session.fail()` permanently marks chat session failed after transient errors | Major |
| #27 | Extend `add_assistant_message` for tool-call responses before Phase 2 | Minor/Phase 2 |
| #28 | Add `name: NotRequired[str]` to `ToolMessage` TypedDict | Minor |
| #29 | Add `LLMClient.__init__` test coverage for model-override + provider detection | Minor |
| #30 | Fix `_history` partial-ID lookup to use SQL `LIKE` query | Minor |
| #31 | `_deserialize_message` silently swallows corrupt `tool_calls` JSON | Minor |
| #32 | Remove or wire up unused `structlog` dependency | Minor |
| #33 | Make `get_settings()` singleton thread-safe | Minor |
| #34 | Fix flaky `test_list_sessions_ordered_newest_first` | Minor |
