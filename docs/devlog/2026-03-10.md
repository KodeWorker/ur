# 2026-03-10

## Done

- Phase 2 structural design completed
- Roadmap updated: Phase 5 → Memory & Context, Phase 6 → Docker Sandbox (swapped)
- New phase5.md created: context compression, long-term vector memory, context usage display
- Phase 2 abstractions scaffolded and formatted:
  - `src/ur/llm/provider.hpp` — `Message` struct + `Provider` abstract interface
  - `src/ur/llm/http_provider.hpp/.cpp` — `HttpProvider` + `make_http_provider()` stubs
  - `src/ur/agent/runner.hpp/.cpp` — `Runner` + `RunResult` stubs
  - `src/ur/cli/command.hpp/.cpp` — `cmd_run` declaration and stub
  - `src/ur/main.cpp` — `run` dispatch + usage line
  - `tests/unit/test_http_provider.cpp` — 6 stubbed tests (loopback server pattern)
  - `tests/unit/test_runner.cpp` — 5 stubbed tests (MockProvider pattern)
- CMake updated: FetchContent for cpp-httplib v0.18.1 and nlohmann/json v3.11.3
- All pre-commit hooks passing
- `Database` extended with `insert_session` / `insert_message`:
  - Declarations added to `src/ur/memory/database.hpp` with doc comments
  - Stubs with detailed TODO comments added to `src/ur/memory/database.cpp`
  - Content field uses `sqlite3_bind_blob` (explicit size) to handle encrypted binary bytes correctly

## Also done

- Added `env.hpp` / `env.cpp` — `load_dotenv()` reads `.env` from cwd
  - Parses `KEY=VALUE`, skips blank lines and `#` comments, strips surrounding quotes
  - Always overwrites existing env vars
  - Silent no-op if `.env` absent
  - Called in `main()` before `make_context()`
- Canonical env var for model name confirmed as `UR_LLM_MODEL_NAME` (not `UR_LLM_MODEL`)
  - Stale `std::getenv("UR_LLM_MODEL")` inside `HttpProvider::complete()` stub must be removed when implementing

## TODOs (core logic — owner: Kodeworker)

### `src/ur/llm/http_provider.cpp`
- [x] `make_http_provider()` — read `UR_LLM_BASE_URL` / `UR_LLM_API_KEY` from env
- [x] `HttpProvider::complete()` — build JSON body, POST via cpp-httplib, parse response
  - Remove stale `std::getenv("UR_LLM_MODEL")` line; use the `model` parameter directly
  - Model resolution (`--model` → `UR_LLM_MODEL_NAME` → `""`) is `cmd_run`'s responsibility

### `src/ur/memory/database.cpp`
- [x] `insert_session()` — prepare/bind/step/finalize INSERT INTO session
- [x] `insert_message()` — prepare/bind/step/finalize INSERT INTO message (blob for content)

### `src/ur/agent/runner.cpp`
- [ ] `Runner::generate_id()` — 16-byte `RAND_bytes()` → 32-char hex string
- [ ] `Runner::run()` — init_schema, generate IDs, INSERT session, build messages,
      call provider, INSERT user + assistant messages (encrypted), return RunResult

### `src/ur/cli/command.cpp`
- [ ] `cmd_run()` — parse args (prompt, --model, --system-prompt), construct provider
      and runner, call runner.run(), print response

### `tests/unit/test_http_provider.cpp`
- [ ] Add loopback httplib::Server fixture (SetUp/TearDown)
- [ ] `CompletePostsToCorrectEndpoint`
- [ ] `CompleteReturnsAssistantContent`
- [ ] `CompleteThrowsOnNon200Response`
- [ ] `CompleteThrowsOnConnectionFailure`
- [ ] `CompleteIncludesAuthHeader`
- [ ] `MakeHttpProviderTest.ReadsBaseUrlFromEnv`

### `tests/unit/test_runner.cpp`
- [ ] Add temp dir fixture (SetUp/TearDown)
- [ ] `RunReturnsProviderResponse`
- [ ] `RunPersistsSessionRow`
- [ ] `RunPersistsTwoMessageRows`
- [ ] `RunPassesSystemPromptAsFirstMessage`
- [ ] `RunEncryptsContentWithKey`
